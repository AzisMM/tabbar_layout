import 'package:flutter/material.dart';

class TabOne extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Tampilan Tab One'),
      ),
    );
  }
}
