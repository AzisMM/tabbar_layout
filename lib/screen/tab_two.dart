import 'package:flutter/material.dart';

class TabTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Tampilan Tab Two'),
      ),
    );
  }
}
